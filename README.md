# SY マニホールド選定・仕様書生成ツール

SMC SYシリーズ（SY3000/5000/7000）プラグインマニホールドの選定ツール。
ブラウザで `smc_sy_plugin_v10.html` を開くだけで動作（**完全オフライン・外部通信ゼロ**）。

## ファイル
- `smc_sy_plugin_v10.html` — **最新版**。品番生成をカタログ手配例と全件突合済み
- `smc_sy_plugin_v9 (2).html` / `smc_sy_plugin_v8 (1).html` — 旧版（品番生成に既知バグあり・参照用）
- `tests/test_catalog_examples.mjs` — カタログ実在品番34件との突合テスト（`node tests/test_catalog_examples.mjs`）

## v10 での修正点（カタログ 7-1-2-p0387-0722 で全件検証）
- **品番生成の修正**
  - 全品番に混入していた余計な `-01`（サブプレート口径）セグメントを除去
  - 取付方法記号はハイフンなしで口径記号に連結（例 `C6AA`, `C6D0`, `01F`）
  - EX600 の ⑤I/Oユニット連数を品番に反映（例 `S6Q72`）。EP(CIP Safety)は④⑤なし
  - EX250 に入力ブロック仕様記号（A〜F）を追加（例 `SQ1C`）
  - 12/52/M12型（上配管）はポートセグメントなし（`SS5Y3-12F1-05D` 形式）
  - 金属ベース: 配線仕様Sは接続ブロック末尾（`50F2S`）、外部パイロットは型式側R（`50R`）、ねじ種類F/Nは口径直後
  - 混合取付: シリーズは5(3000/5000混合)/7(5000/7000混合)、口径は2桁記号（`C86`/`C128`）
  - バルブ品番: 配管位置digit（0=ベース配管/3=上配管）と外部パイロットR接尾を修正
    （`SY3201-5U1`メタル・`SY3200R-5U1`外部・`SY3130-5U1-C6`上配管が正しく出る）
- **データ修正**: EX250のTA〜TD=AS-Interface/ZE=EtherNet/IP、EX260のSI記号表（QA〜CAN・極性・点数・コネクタ）、連数上限（点数と連数の混同を解消）
- **完全オフライン化**: XLSXライブラリをインライン埋め込み、Google Fonts削除（システムフォント化）
- **UI改善**
  - 品番バーに未入力項目ガイド（クリックで該当ステップへジャンプ）
  - バルブ連数ステップにソレノイド数メーター（シングル=1点/ダブル系=2点、配線方式の上限と照合）
  - 構成の保存/読込（JSONファイル）＋ localStorage 自動保存・「↺ 前回」復元
  - Excel出力に「マニホールド仕様書」シート（連No.×品番×配線指定グリッド）と
    「手配集約」シート（＊組込み記号付き・同一品番set数まとめ、カタログ手配例形式）を追加

## テスト
```
node tests/test_catalog_examples.mjs
```
マニホールドベース品番27件＋バルブ品番7件をカタログ手配例と突合（34/34 pass）。
データ修正時は必ずこのテストを通すこと。

## 今後
このUI・構造をベースに JSY シリーズ版マニホールド仕様書作成ソフトを作成予定
（データソース: S11-110-JSY.pdf / JSY_plugin_valid_models.xlsx）。

## SMC公式WEBカタログ突合（2026-07-25）
SMC公式WEBカタログの「品番確認」（`webcatalog/s3s/ja-jp/detail/?partNumber=`）を判定器として、
ツールが生成しうる品番を網羅生成し（マニホールド459種・バルブ192種）SMC側と突合。
不一致をカタログ実ページで確認したうえで、以下の**カタログ制約9件**を強制するようにした
（JSY版 `jsy-manifold-tool` と共通の修正。EX250の入力ブロック上限のみ数値が異なる）。

| # | 制約 | 修正前の症状 |
|---|------|--------------|
| 1 | DINレール長さ□はバルブ連数より長いこと | 5連でD6選択→10連に増連してもD6が残留し不正品番 |
| 2 | EX250 SIユニットなし(S0)はDINレール取付不可 | `…S0-…D` を生成 |
| 3 | EX600 SIユニットなし(S60)はD0のみ（レール付Dは不可） | 警告表示のみで強制せず `…S60-…D` を生成 |
| 4 | EX245 SIユニットなし(0)は入出力モジュール④⑤とも無記号 | `…S0Y3-…` を生成 |
| 5 | EX250 入力ブロック連数上限（TA/TC=4連・TB/TD=2連） | `…STA5A-…` 等の上限超過を生成 |
| 6 | EX600 FB（PROFINET OPC UA対応）はマイナスコモンのみ | プラスコモン(2/3/6/8)を選択でき `…S6FB2-…` を生成 |
| 7 | EX600 ④無記号はSIユニットなしのみ | 読込/逆引き経由で `…S6DA-…`（④欠落）が残りうる |
| 8 | EX600 EP（CIP Safety）はマイナスコモン(PNP) | UI表示3箇所が「プラスコモン(NPN)」と逆（ロジックは元から正） |
| 9 | 節電コイル(T)はZ/NZのみ（U=無極性の設定はない） | サニタイズ未実装でJSON読込経由の `SY□□00T-5U1` が残りうる |

- SUP./EXH.ブロッキングディスクの品番をSMC側で確認（SY3000: SUP=SY30M-40-1A / EXH=SY30M-40-2A、
  SY5000・7000: SUP=EXH=SY□0M-40-1A）。**SY5000/7000はEXHに同一品番を2個**必要
  （カタログ「EA/EB両側のEXH.をブロックする場合、2ヶ必要になります」）ため、BOM・手配集約・
  構成製品リストで2個計上するよう修正した。
- 回帰テスト: `tests/test_sy_constraints.mjs`（27件）
- **JSY版との差**: EX250のAS-Interface入力ブロック上限は、SY側は入力ブロック仕様（2点/4点）に
  依存せずTA/TC=4連・TB/TD=2連。JSY側は入力点数ベース（TA/TC≤8点・TB/TD≤4点）。
  どちらもカタログは「最大連数に制限があります」としか記載せず、SMCの品番DBの受理範囲に合わせている。
